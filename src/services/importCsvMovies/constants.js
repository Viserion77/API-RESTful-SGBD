// @ts-check

const CSV_HEADERS = ['year', 'title', 'studios', 'producers', 'winner']
const CSV_SEPARATOR = ';'
const INVALID_CSV_HEADERS = 'Invalid CSV headers'
const TO_SCAN_DIRECTORY = './toScan'

module.exports = {
  CSV_HEADERS,
  CSV_SEPARATOR,
  INVALID_CSV_HEADERS,
  TO_SCAN_DIRECTORY,
}
